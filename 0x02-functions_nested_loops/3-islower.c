#include"main.h"

/**
 * main - Entry point
 *
 * _islower - print if the number is a lower character
 * c the parameter
 *           
 *
 * Return: Always 0 (Success)
*/

int _islower(int c)
{
      if(c >= 97 && c <= 122)
      {
            return (1);
      }
      else
      {
            return (0);
      }  
}      
