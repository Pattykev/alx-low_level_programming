#include"main.h"

/**
 * main - Entry point
 *
 * Description: print if the number is a lower character
 *           
 *
 * Return: Always 0 (Success)
*/

int _abs(int n)
{
      if (n < 0)
      {
            n=n*(-1);
      }  
      return n;
}
